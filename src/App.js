/**
 * Reusable Component
 * <App />
 *
 * Start your component name with Capital Letter
 *
 * Keep your File Name, Start with Capital
 *
 * We Try to put All our HTML inside ()
 * JSX () => Used to Wrtie HTML inside JS.
 *
 * For only one TAG it is OPTINAL :: Try to correate with lamda
 */
function App() {
  return (
    <div>
      <div>ABCD</div>
    </div>
  );
}

export default App;
