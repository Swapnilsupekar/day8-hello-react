import "./App.css";

/**
 * class :: className
 * style :: by using object literals {}
 */
function App() {
  return (
    <div
      style={{
        fontSize: "2rem",
        backgroundColor: "royalblue",
        color: "white",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Matrix 4
    </div>
  );
}

export default App;
